# Task 1.1: Simple Basic API Implementation

## Create Simple Task CRUD Endpoints

Since you already have the project structure and health check, let's create **very basic** CRUD endpoints without complex Pydantic models.

## Step 1: Create Simple Task Router

Create `app/routers/tasks.py`:

```python
from fastapi import APIRouter, HTTPException
from typing import List, Dict, Any

# Create router instance
router = APIRouter(prefix="/tasks", tags=["tasks"])

# Simple in-memory storage (just a list of dictionaries)
tasks_db = []
task_id_counter = 1

@router.get("/")
async def get_tasks():
    """Get all tasks"""
    return {"tasks": tasks_db, "total": len(tasks_db)}

@router.post("/")
async def create_task(task_data: dict):
    """Create a new task"""
    global task_id_counter

    # Create simple task with basic fields
    new_task = {
        "id": task_id_counter,
        "title": task_data.get("title", ""),
        "description": task_data.get("description", ""),
        "completed": task_data.get("completed", False)
    }

    tasks_db.append(new_task)
    task_id_counter += 1

    return {"message": "Task created successfully", "task": new_task}

@router.get("/{task_id}")
async def get_task(task_id: int):
    """Get a specific task by ID"""
    # Find task by ID
    task = None
    for t in tasks_db:
        if t["id"] == task_id:
            task = t
            break

    if not task:
        raise HTTPException(status_code=404, detail="Task not found")

    return {"task": task}

@router.put("/{task_id}")
async def update_task(task_id: int, task_data: dict):
    """Update a specific task by ID"""
    # Find task by ID
    task = None
    task_index = None
    for i, t in enumerate(tasks_db):
        if t["id"] == task_id:
            task = t
            task_index = i
            break

    if not task:
        raise HTTPException(status_code=404, detail="Task not found")

    # Update task fields
    if "title" in task_data:
        task["title"] = task_data["title"]
    if "description" in task_data:
        task["description"] = task_data["description"]
    if "completed" in task_data:
        task["completed"] = task_data["completed"]

    tasks_db[task_index] = task

    return {"message": "Task updated successfully", "task": task}

@router.delete("/{task_id}")
async def delete_task(task_id: int):
    """Delete a specific task by ID"""
    # Find task by ID
    task_index = None
    for i, t in enumerate(tasks_db):
        if t["id"] == task_id:
            task_index = i
            break

    if task_index is None:
        raise HTTPException(status_code=404, detail="Task not found")

    deleted_task = tasks_db.pop(task_index)

    return {"message": "Task deleted successfully", "deleted_task": deleted_task}
```

## Step 2: Update Main Application

Update your `app/main.py` to include the task router:

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

# Import the task router
from app.routers import tasks

# Create FastAPI instance
app = FastAPI(
    title="Task Management API",
    description="A simple task management API built with FastAPI",
    version="1.0.0"
)

# Add CORS middleware
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# Include the tasks router
app.include_router(tasks.router)

# Health check endpoint (you should already have this)
@app.get("/health")
async def health_check():
    return {"status": "healthy", "message": "Task Management API is running!"}

# Root endpoint
@app.get("/")
async def root():
    return {"message": "Welcome to Task Management API", "docs": "/docs"}
```

## Step 3: Create Router Init File

Create `app/routers/__init__.py`:

```python
# This file makes routers a Python package
```

## Step 4: Test Your Simple API

### Start the server:

```bash
uvicorn app.main:app --reload
```

### Test the endpoints:

**1. Visit the documentation:**

- Go to `http://localhost:8000/docs`

**2. Test with simple curl commands:**

```bash
# 1. Get all tasks (should be empty initially)
curl -X GET "http://localhost:8000/tasks/"

# 2. Create a task
curl -X POST "http://localhost:8000/tasks/" \
  -H "Content-Type: application/json" \
  -d '{"title": "My first task", "description": "Learning FastAPI", "completed": false}'

# 3. Get all tasks (should show your created task)
curl -X GET "http://localhost:8000/tasks/"

# 4. Get specific task (replace 1 with the actual ID)
curl -X GET "http://localhost:8000/tasks/1"

# 5. Update the task
curl -X PUT "http://localhost:8000/tasks/1" \
  -H "Content-Type: application/json" \
  -d '{"title": "Updated task", "completed": true}'

# 6. Delete the task
curl -X DELETE "http://localhost:8000/tasks/1"
```

**3. Test in the browser:**

- Go to `http://localhost:8000/docs`
- Try each endpoint using the "Try it out" button

## What This Simple Implementation Includes:

✅ **All Required CRUD Endpoints:**

- `GET /tasks` - List all tasks
- `POST /tasks` - Create a task
- `GET /tasks/{task_id}` - Get specific task
- `PUT /tasks/{task_id}` - Update task
- `DELETE /tasks/{task_id}` - Delete task

✅ **Basic Features:**

- Simple in-memory storage (list of dictionaries)
- Basic error handling (404 for not found)
- Path parameters (`{task_id}`)
- JSON request/response
- Automatic API documentation

✅ **What You're Learning:**

- FastAPI routing basics
- Path operations (GET, POST, PUT, DELETE)
- Path parameters
- Basic request/response handling
- HTTP status codes
- Router organization

## Task 1.1 Completed! ✅

This implementation is intentionally simple because:

- **No complex Pydantic models** (that's for Task 1.2)
- **No advanced validation** (that's for Task 1.2)
- **No complex response models** (that's for Task 1.2)
- **Just basic CRUD operations** with simple data structures

Perfect for learning the fundamentals before moving to more advanced concepts!

## Example Usage:

```python
# Sample task creation
{
  "title": "Learn FastAPI",
  "description": "Complete the FastAPI tutorial",
  "completed": false
}

# Sample response
{
  "message": "Task created successfully",
  "task": {
    "id": 1,
    "title": "Learn FastAPI",
    "description": "Complete the FastAPI tutorial",
    "completed": false
  }
}
```

Now you can move on to **Task 1.2** where you'll enhance this with proper Pydantic models and validation! 🚀
